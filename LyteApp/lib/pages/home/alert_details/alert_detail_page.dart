import 'package:LyteApp/models/alert.dart';
import 'package:flutter/material.dart';

class AlertDetailPage extends StatelessWidget {
  AlertDetailPage(String id);

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      body: new Stack(
        children: <Widget>[],
      ),
    );
  }
}
